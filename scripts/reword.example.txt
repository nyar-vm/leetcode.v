# Hash-keyed reword map example. Only listed commits are rewritten during rebase.

3eba9520e
✨ Extract `v-bench.ts` from `bench-all.ts` for per-problem compile timing

Split valkyrie and TypeScript bench runners out of `bench-all.ts` so each problem row records reference and legion compile timing without vcc `runBenchmarkSuite`.

---

dc91d0a1
📚 Expand bench fairness and valkyrie invoke guidance in `AGENTS` and skills

Document Python in-process `metadata.tests` timing parity, wasm invoke backlog anchors, and valkyrie `callExport` golden path notes in `AGENTS.md` and agent skills.

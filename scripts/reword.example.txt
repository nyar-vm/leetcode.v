✨ Complete `two-sum` coach batch with hash map lookup

Add Chinese coach readme and TypeScript and Valkyrie solvers aligned with the readme reference implementation.

---

🐛 Guard `merge-sorted-array` writes with explicit bounds checks

Throw on out-of-range writes so metadata `Error` cases pass instead of relying on length-only guards.
